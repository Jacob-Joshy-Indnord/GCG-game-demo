# Defines plugins to load (macOS — Metal backend)

# Define plugin folder
PluginFolder=.

# Metal is the primary (and only recommended) RenderSystem on macOS.
# Apple has deprecated OpenGL, so GL3Plus is intentionally omitted.
Plugin=RenderSystem_Metal
Plugin=Plugin_ParticleFX
